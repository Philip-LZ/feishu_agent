# 已掌握按 routing key 调度的基本语义

用户能正确写出私聊、群聊与 thread 的 routing key，说明有界队列满载时当前实现记录 warning 后丢弃消息且调用方无法从返回值识别，并能完整复述 idle timeout 与新消息交错导致丢失的五步竞态。后续需继续巩固 `_dispatch_lock` 保护的是共享调度状态而非 Agent 执行，以及必须从真实 `tests/` 文件而非设计文档判断测试覆盖。

## Evidence

用户完成第二课练习；前三项核心运行语义和竞态时序正确，原子边界与测试证据经本轮纠正。
